```
p_eff  = p̂ / (1 + ε₂F)
rate   = 5 / τ
s(D_del) = s₀ + s_d D_del / (1 + θ_s D_del)

dT/dt   = −β T V
dI/dt   = β T V − δ I
dV/dt   = (1−d) p_eff I − c V
dD/dt   = d p_eff I − h D
dF/dt   = s(D_del) I_del − α F
dZI_i/dt = rate (ZI_{i−1} − ZI_i),  i = 1..5
dZD_i/dt = rate (ZD_{i−1} − ZD_i),  i = 1..5
```
